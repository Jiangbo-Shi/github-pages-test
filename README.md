# GitHub Pages Test

This is a tiny static site used to verify the GitHub Pages publishing flow.

The site entry point is `index.html`.

Deploys through `.github/workflows/pages.yml` after pushing to `main`.
